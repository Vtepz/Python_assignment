# Legacy SQL files

`schema.sql` and `database.sql` predate the current SQLAlchemy models and no longer match `backend/models.py` (which defines only `employees` and `users`). Kept for reference only — not used by the app. `models.py` is the source of truth for the schema.
