# Leave VITE_API_URL unset in production — vercel.json proxies /api/* to the
# Render backend, so the frontend just calls its own origin.
# Only set this for local dev if you want to bypass the Vite dev proxy.
# VITE_API_URL=http://localhost:5000/api
